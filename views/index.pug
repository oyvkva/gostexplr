extends layout

block content
  h3 Last 100 blocks

  table
    each block in blocks
      tr
        td #{block.height}
        td #{block.time}
        td #{block.difficulty}
        td
          a(href='/block/' + block.hash + '/') #{block.hash}
